import express from "express";
export const router = express.Router();

// for all
router.get('/', )
router.post('/', )
router.get('/:userid', )

//for only owner
router.get('/w', )
router.get('/w/:id', )
router.put('/w/:id', )
router.delete('/w/:id', )

