import express from "express";
export const router = express.Router();

router.get('/', )
router.get('/:id', )

// for businesses and admins
router.post('/', )
router.put('/:id', )
router.delete('/:id', )
