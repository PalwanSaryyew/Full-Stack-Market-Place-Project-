import express from "express";
export const router = express.Router();

router.get('/', )
router.get('/:id', )

// for admins and businesses
router.post('/', )
router.put('/:id', )
router.delete('/:id', )
