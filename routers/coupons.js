import express from "express";
export const router = express.Router();

//for all
router.get('/', )
router.get('/:business', )

// for only businesses and admins
router.post('/', )
router.put('/c/:id', )
router.delete('/c/:id', )
